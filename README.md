# eka-leka-flutter

Introduces the PyInstaller spec file, build artifacts, and distribution files for the FlutterInstaller project. Includes the main application script, PowerShell installer, and all necessary compiled and packaged resources for Windows deployment.


\* You can run directly the script using powershell as admin

* use the python script
